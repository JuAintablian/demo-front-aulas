import { ValditorDirective } from './valditor.directive';

describe('ValditorDirective', () => {
  it('should create an instance', () => {
    const directive = new ValditorDirective();
    expect(directive).toBeTruthy();
  });
});
